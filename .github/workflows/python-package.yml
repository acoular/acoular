# This workflow will install acoular and run tests a variety of Python versions

name: Acoular install and test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ${{ matrix.os }} 

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-13, windows-latest]
        python-version: ['3.7', '3.8', '3.9','3.10','3.11']

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install acoular
      run: |
        python -m pip install --upgrade pip
        pip install .
        python -c 'import acoular'
    - name: Test ubuntu with unittest Linux
      run: |
        cat /proc/cpuinfo
        chmod +x tests/run_tests.sh
        cd tests
        bash run_tests.sh
      if: ${{ matrix.os == 'ubuntu-latest'}} 
    - name: Test macos-13 with unittest Unix
      run: |
        sysctl -a machdep.cpu
        chmod +x tests/run_tests.sh
        cd tests
        bash run_tests.sh
      if: ${{ matrix.os == 'macos-13'}}
    - name: Test with unittest Windows
      run: |
        systeminfo
        cd tests
        python -m unittest discover -v -p "test_*.py"
      if: ${{ matrix.os == 'windows-latest'}}


