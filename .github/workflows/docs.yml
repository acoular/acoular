name: "Manual: Docs 📝 Build 🛠️ & Deploy"
run-name: Build Docs 🛠️ & Publish to GitHub Pages 📝
permissions:
  contents: write
on:
  workflow_dispatch:

jobs:
  build_and_deploy:
    uses: acoular/ci/.github/workflows/docs.yml@main
    with:
      deploy: true
