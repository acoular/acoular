name: "Cron 🕑: Check Installation & Links"
permissions:
  contents: read
on:
  schedule:
    - cron: "00 18 * * *"
  workflow_dispatch:


jobs:
  install:
    uses: acoular/ci/.github/workflows/install.yml@main

  link-check:
    uses: acoular/ci/.github/workflows/links.yml@main
