.. tab-set::
    :sync-group: tool

    .. tab-item:: ``uv``
        :sync: uv

        .. code-block:: console

            $ uv run python -m pytest --doctest-modules acoular

    .. tab-item:: ``pip``
        :sync: pip

        .. code-block:: console

            $ python -m pytest --doctest-modules acoular

    .. tab-item:: ``mamba``
        :sync: mamba

        .. code-block:: console

            $ python -m pytest --doctest-modules acoular

    .. tab-item:: ``conda``
        :sync: conda

        .. code-block:: console

            $ python -m pytest --doctest-modules acoular
