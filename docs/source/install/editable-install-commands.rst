.. tab-set::
    :sync-group: tool

    .. tab-item:: ``uv``
        :sync: uv

        .. note::

           ``uv`` will make an editable installation of Acoular automatically. So nothing needs to be done here.

    .. tab-item:: ``pip``
        :sync: pip

        .. code-block:: console

            $ pip3 install -Ue .'[full]' --group dev
            
